package testingjava;

public class SubString {
    private static boolean subString(String mainString,String sub ){
        boolean flag=false;
        for (int i=0;i<mainString.length();i++){
            for (int j=0;j<sub.length();j++){

            }
        }

        return flag;
    }
}
